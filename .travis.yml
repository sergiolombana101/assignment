#https://github.com/sergiolombana101

language: node_js

node_js:
  - "10.9.0"

cache: npm

env:
  - NOW_TOKEN="VRihodwu81gcAWzokpySNczc"

before_install:
  - ls -la
  - "npm config set spin false"
  - npm i -g now

install:
  - npm install

script:
  - npm run build
  - npm run test

deploy:
  provider: script
  script: bash scripts/deploy.sh
  skip_cleanup: true